FROM python:3.11-slim
RUN apt-get update && apt-get install -y r-base r-cran-tidyverse
COPY requirements.txt .
RUN pip install -r requirements.txt
COPY . /app
WORKDIR /app
RUN pip install -e .
CMD ["python", "scripts/run_calibration.py"]
